[build]
  publish   = "."
  functions = "netlify/functions"

# Pass Sheets CSV fetches through (CORS is already open on Google's end)
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options    = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
